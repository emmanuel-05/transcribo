# Configuration Base de Données (pointant vers le service postgres de Docker)
DATABASE_URL=postgresql+asyncpg://transcribo:transcribo123@postgres:5432/transcribo

# Configuration Redis (pointant vers le service redis de Docker)
REDIS_URL=redis://redis:6379

# Sécurité
SECRET_KEY=votre_cle_secrete_super_securisee

# Clés d'API Externes (à remplir)
DEEPGRAM_API_KEY=
GROQ_API_KEY=
CONVERTIO_API_KEY=

# Configuration S3 / MinIO (pointant vers le service minio de Docker)
S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
